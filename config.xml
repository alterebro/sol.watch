<?xml version="1.0" encoding="UTF-8"?>
<widget xmlns     = "http://www.w3.org/ns/widgets"
        xmlns:gap = "http://phonegap.com/ns/1.0"
        id        = "com.alterebro.solwatch"
        version   = "1.0.0">

    <name>Sol Watch. Geolocated Sun Clock</name>
    <description>
		SolWatch is a Sun clock that visualizes the Solar phases depending on your current location. It doesn't give you the time but instead it shows the rotation degrees left to the next Solar Event based on the geographical position of the request.
    </description>
    <author href="http://moro.es">
        Jorge Moreno
    </author>

    <preference name="permissions"                value="none"/>
    <preference name="orientation"                value="portrait" />        <!-- all: default means both landscape and portrait are enabled -->
    <preference name="target-device"              value="universal" />      <!-- all: possible values handset, tablet, or universal -->
    <preference name="fullscreen"                 value="false" />           <!-- all: hides the status bar at the top of the screen -->
    <preference name="android-installLocation"    value="auto" />           <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->

	<gap:plugin name="org.apache.cordova.geolocation" />

    <icon src="assets/images/favicon.png" />
    <access origin="*"/>
    <allow-intent href="http://*/*" />
    <allow-intent href="https://*/*" />
    <allow-intent href="geo:*" />
    <platform name="android">
        <allow-intent href="market:*" />
    </platform>
</widget>
